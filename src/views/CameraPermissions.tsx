import React from "react";
import { View } from "react-native";
import { PatientNavigationProps } from "~models/types";

const CameraPermissions = ({ route, navigation }: PatientNavigationProps) => {
  return (
    <View>
      
    </View>
  );
};

export default CameraPermissions;
